apiVersion: v1
kind: Service
metadata:
  name: njil
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "njil"
    port: 8080
  selector:
    app: njil
